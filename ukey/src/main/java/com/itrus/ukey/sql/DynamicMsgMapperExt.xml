<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itrus.ukey.db.DynamicMsgMapper">

	<select id="selectMaxPublish" resultMap="BaseResultMap">
		select
		  project,
		  MAX(publish_time) as publish_time
		from
		  dynamic_msg
		where
		  publish_type = 0
		GROUP BY
		  project
	</select>
	<select id="selectByExampleOfLimit" resultType="java.util.Map"
		parameterType="com.itrus.ukey.db.DynamicMsgExample">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. -->
		select
		<if test="distinct">
			distinct
		</if>
			msg_link as msgLink,
			msg_title as msgTitle,
			publish_time as publishTime
		from dynamic_msg
		<if test="_parameter != null">
			<include refid="Example_Where_Clause" />
		</if>
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
	</select>
</mapper>